- name: Demo on Loops
  hosts: localhost
  connection: local
  tasks:
    - name: Print Fruit name
      ansible.builtin.debug:
        msg: Fruit Name - {{ item }}
      loop:
        - banana
        - apple
        - orange
        - apple