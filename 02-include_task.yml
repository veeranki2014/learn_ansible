- name: Include task Example
  hosts: all
  tasks:
    - name: Print debug
      ansible.builtin.debug:
        msg: print Debug msg

    - name: Include a file
      ansible.builtin.include_tasks:
        file: universal.yml